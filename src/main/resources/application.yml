server:
  port: 8082
spring:
  application:
    name: stripe-payment-provider
  jackson:
    property-naming-strategy: SNAKE_CASE
stripe:
  config:
    mode: payment
    successUrl: http://localhost:8080/payment/success
    cancelUrl: http://localhost:8080/payment/cancel
    createLinkAPI: https://api.stripe.com/v1/checkout/sessions
    secret-key: key